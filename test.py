integer_str = input("enter a number ")
integer_length = len(integer_str)
max_index = integer_length - 1
# first loop to the lowest from the highest from the reverse
for i in range(integer_str[max_index], integer_str[0], -1):
    

